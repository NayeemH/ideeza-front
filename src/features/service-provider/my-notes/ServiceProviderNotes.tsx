import React from 'react';
import Notes from '@features/technician/notes';

const ServiceProviderNotes = () => {
	return (
		<div>
			{/* here notes component should receive differnt props to work as a dynamic component */}
			<Notes />
		</div>
	);
};

export default ServiceProviderNotes;
